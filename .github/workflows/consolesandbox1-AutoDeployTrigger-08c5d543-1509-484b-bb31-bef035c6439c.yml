name: Trigger auto deployment for consolesandbox1

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ master ]
    paths:
    - '**'
    - '.github/workflows/consolesandbox1-AutoDeployTrigger-08c5d543-1509-484b-bb31-bef035c6439c.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.CONSOLESANDBOX1_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.CONSOLESANDBOX1_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.CONSOLESANDBOX1_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}
          _dockerfilePathKey_: _dockerfilePath_
          _targetLabelKey_: _targetLabel_
          registryUrl: mycontainerapp8.azurecr.io
          registryUsername: ${{ secrets.CONSOLESANDBOX1_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.CONSOLESANDBOX1_REGISTRY_PASSWORD }}
          containerAppName: consolesandbox1
          resourceGroup: DefaultResourceGroup-WEU
          imageToBuild: mycontainerapp8.azurecr.io/consolesandbox1:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


